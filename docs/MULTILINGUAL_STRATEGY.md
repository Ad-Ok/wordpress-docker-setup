# 🌍 Стратегия многоязычности для сайта Maslovka

**Дата:** 5 ноября 2025  
**Статус:** Рекомендации к внедрению  
**Языки:** Русский (по умолчанию) + Английский

---

## 📋 Текущая ситуация

### Готовая структура:
- ✅ ACF Pro для кастомных полей
- ✅ 3 кастомных типа постов: Collections, Vistavki, Sobitiya
- ✅ 1 кастомная таксономия: Events
- ✅ ~11 ACF групп полей
- ✅ Slim SEO для SEO
- ✅ Структура готова к деплою

### Вывод:
**Самое время внедрять многоязычность!** Структура готова, контента еще мало.

---

## 🎯 Рекомендуемое решение: Polylang PRO

### Почему Polylang PRO:

1. **✅ Полная интеграция с ACF Pro** - официальная поддержка перевода всех кастомных полей
2. **✅ Автоматический перевод таксономий** - categories, tags, custom taxonomies
3. **✅ Совместимость с Slim SEO** - уже есть интеграция (обнаружено в коде)
4. **✅ SEO-friendly** - отдельные URL для каждого языка (`/ru/`, `/en/`)
5. **✅ WP REST API support** - если планируете headless в будущем
6. **✅ Относительно недорого** - €99/год для 1 сайта

### Альтернативы (не рекомендуются для этого проекта):

| Плагин | Цена | Плюсы | Минусы |
|--------|------|-------|--------|
| **Polylang PRO** ⭐ | €99/год | ACF, таксономии, SEO | Требует ручной перевод |
| **WPML** | $99/год | Мощный, автоперевод | Тяжелый, дороже с дополнениями |
| **TranslatePress** | $89/год | Визуальный редактор | Хуже с ACF и кастомными полями |
| **Weglot** | от $99/мес | Автоперевод, простой | Дорого, зависимость от облака |

---

## 🗓️ КОГДА внедрять многоязычность

### ✅ ОДНОЗНАЧНО ДО создания большого количества контента

**Почему СЕЙЧАС идеально:**

```
✅ СЕЙЧАС (структура есть, контента мало)
├─ Настроите все типы постов как переводимые
├─ Настроите все таксономии как переводимые
├─ Настроите все ACF поля как переводимые
└─ Начнете наполнять контент СРАЗУ на обоих языках

❌ ПОТОМ (если заполните контент на одном языке)
├─ Придется дублировать 100+ записей вручную
├─ Возможны проблемы с связями между постами
├─ Сложнее отследить что переведено, а что нет
└─ Больше работы = больше ошибок
```

### Рекомендуемая последовательность:

```
1. ✅ Этап 1: Подготовка (ДО production-запуска)
   ├─ Установить и настроить Polylang PRO
   ├─ Настроить языки и URL-структуру
   ├─ Включить переводы для CPT и таксономий
   └─ Настроить ACF для переводов

2. ✅ Этап 2: Базовый контент (минимум до production)
   ├─ Перевести структуру (меню, таксономии)
   ├─ Перевести основные страницы
   └─ Перевести названия кастомных полей в админке

3. 🔄 Этап 3: Наполнение (можно постепенно после production)
   ├─ Перевод всех коллекций
   ├─ Перевод выставок
   └─ Перевод событий
```

---

## 🔗 Организация URL - КРИТИЧЕСКИ ВАЖНО

### ❓ Вопрос: Можно ли использовать `?lang=en` вместо `/en/`?

**Ответ: Технически возможно, но КРАЙНЕ не рекомендуется!**

### Сравнение вариантов:

| Вариант | Пример | SEO | Кеширование | Рекомендация |
|---------|--------|-----|-------------|--------------|
| **Поддомен** | `en.maslovka.org` | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | Для больших проектов |
| **Поддиректория** | `maslovka.org/en/` | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | **👍 ЛУЧШИЙ ВЫБОР** |
| **Параметр** | `maslovka.org?lang=en` | ⭐⭐ | ⭐⭐ | ❌ Плохо для SEO |
| **Cookie/session** | `maslovka.org` | ⭐ | ⭐ | ❌ Очень плохо |

### Почему параметр `?lang=en` - плохая идея:

```
❌ Проблемы с параметрами в URL:
1. Google плохо индексирует страницы с параметрами
2. Невозможно правильно настроить hreflang-теги
3. Проблемы с кешированием (Nginx, CDN, браузеры)
4. Проблемы с аналитикой (смешиваются метрики)
5. Пользователи не могут сохранить закладку на нужном языке
6. Социальные сети (VK, FB) не распознают язык контента
7. Поисковые системы считают это дублированием контента
8. Невозможно использовать rel="alternate" правильно
```

### ✅ Рекомендуемая структура URL:

```
Язык по умолчанию: Русский (БЕЗ префикса)
Второй язык: Английский (С префиксом /en/)

Примеры:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Главная:
maslovka.org/                          → 🇷🇺 Главная (RU)
maslovka.org/en/                       → 🇬🇧 Home (EN)

Коллекции:
maslovka.org/kollekcii/                → 🇷🇺 Коллекции (RU)
maslovka.org/en/collections/           → 🇬🇧 Collections (EN)

Конкретная коллекция:
maslovka.org/kollekcii/postoyannaya/   → 🇷🇺 Постоянная экспозиция
maslovka.org/en/collections/permanent/ → 🇬🇧 Permanent Exhibition

Выставки:
maslovka.org/vistavki/                 → 🇷🇺 Выставки (RU)
maslovka.org/en/exhibitions/           → 🇬🇧 Exhibitions (EN)

События и категории:
maslovka.org/events/ekskursii/         → 🇷🇺 Категория: Экскурсии
maslovka.org/en/events/tours/          → 🇬🇧 Category: Tours
```

### Преимущества поддиректорий:

```
✅ SEO-friendly - Google правильно индексирует
✅ Понятные URL для пользователей
✅ Легко делиться ссылками в соцсетях
✅ Работает с CDN и кешированием
✅ Hreflang работает автоматически
✅ Пользователи видят язык в адресной строке
✅ Стандарт индустрии (используют 90% сайтов)
✅ Простая настройка в Google Search Console
```

---

## 🎨 Как будут организованы переводы

### Архитектура базы данных с Polylang:

```
База данных WordPress:
├─ wp_posts (посты хранятся отдельно для каждого языка)
│  ├─ ID: 123 (русский пост "Выставка Импрессионистов")
│  └─ ID: 124 (английский пост "Impressionists Exhibition")
│
├─ wp_term_relationships (связи переводов через термины)
│  └─ Связывает 123 ↔ 124 как переводы друг друга
│
└─ wp_postmeta (ACF поля хранятся отдельно для каждого языка)
   ├─ post_id: 123 → exhibition_date: "2025-12-01"
   ├─ post_id: 123 → exhibition_title: "Импрессионисты"
   ├─ post_id: 124 → exhibition_date: "2025-12-01"
   └─ post_id: 124 → exhibition_title: "Impressionists"
```

### Работа в админке WordPress:

```
WordPress Admin:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📁 Коллекции (Collections)
   ├─ "Постоянная экспозиция" [RU] 🇷🇺 
   │   ├─ [Edit] [Quick Edit] [Trash]
   │   └─ [+ Add translation EN] ← КЛИК = создает английскую версию
   │
   └─ "Permanent Exhibition" [EN] 🇬🇧
       ├─ [Edit] [Quick Edit] [Trash]
       └─ 🔗 Linked to: "Постоянная экспозиция" (RU)

📁 Выставки (Vistavki)
   ├─ "Импрессионисты" [RU] 🇷🇺
   │   └─ [+ Add translation EN]
   │
   └─ "Impressionists" [EN] 🇬🇧
       └─ 🔗 Linked to: "Импрессионисты" (RU)

📁 События → Categories (Таксономия Events)
   ├─ "Экскурсии" [RU] 🇷🇺 ← переводится отдельно
   │   └─ [+ Add translation EN]
   │
   └─ "Tours" [EN] 🇬🇧
       └─ 🔗 Linked to: "Экскурсии" (RU)
```

### Что переводится и как:

#### ✅ Автоматически настраивается (один раз):
- **Custom Post Types** - labels в админке (Коллекции/Collections)
- **Taxonomies** - labels в админке (События/Events)
- **Меню** - создаются отдельные меню для каждого языка
- **Виджеты** - можно назначить на конкретный язык

#### ✏️ Переводится вручную для КАЖДОЙ записи:
- **Заголовки постов** - отдельный пост на каждом языке
- **Содержимое постов** - весь контент дублируется
- **ACF поля** - КАЖДОЕ поле переводится отдельно!
- **Featured Image** - можно использовать одно изображение или разные
- **Мета-описания** (Slim SEO) - отдельные для каждого языка
- **Термины таксономий** - каждая категория/тег переводится

#### 🔧 В коде темы (для разработчика):

```php
// ❌ БЫЛО (хардкод на русском):
echo "Выставки";
echo "Смотреть все коллекции";

// ✅ СТАЛО (с поддержкой переводов):
echo pll__("Выставки");
echo pll__("Смотреть все коллекции");

// Регистрация строк для перевода (в functions.php):
if (function_exists('pll_register_string')) {
    pll_register_string('exhibitions_title', 'Выставки', 'Theme texts');
    pll_register_string('view_all_collections', 'Смотреть все коллекции', 'Theme texts');
}

// Для ACF labels (настраивается один раз при создании поля):
'label' => pll__('Дата выставки')

// Условия по языку в шаблонах:
if (pll_current_language() == 'ru') {
    // Русская версия
} else {
    // Английская версия
}

// Получить URL перевода текущей страницы:
$en_url = pll_get_post(get_the_ID(), 'en');
```

---

## 🚀 План внедрения (Пошаговый)

### Этап 1: Подготовка (2-3 часа)

```bash
1. Покупка и установка
   ├─ Купить Polylang PRO (€99/год) на polylang.pro
   ├─ Скачать .zip плагина
   └─ Установить через WordPress Admin → Plugins → Add New → Upload

2. Базовая настройка языков
   └─ Settings → Languages
      ├─ Add language: Russian (Русский) - DEFAULT
      ├─ Add language: English (English)
      ├─ URL modifications: "The language is set from the directory name in pretty permalinks"
      ├─ Default language: Russian
      ├─ Show all languages: Yes
      └─ Detect browser language: Yes (опционально)

3. Настройка поддиректорий
   └─ Settings → Languages → URL modifications
      ├─ Russian: Hide URL language information for default language
      ├─ English: /en/
      └─ Save changes

4. Включить переводы для кастомных типов
   └─ Settings → Languages → Custom post types and Taxonomies
      ✅ Post Types:
         ├─ [x] Collections (collections)
         ├─ [x] Vistavki (vistavki)
         └─ [x] Sobitiya (sobitiya)
      
      ✅ Taxonomies:
         └─ [x] Events (events)
```

### Этап 2: Интеграция с ACF (30 минут)

```bash
Settings → Languages → ACF
├─ Sync field groups: Enable
└─ Translate field values: Enable

Результат:
- Все ACF поля автоматически становятся переводимыми
- В редакторе поста появятся поля для каждого языка
```

### Этап 3: Меню (15 минут)

```bash
Appearance → Menus
├─ Создать меню: "Main Menu RU"
│  ├─ Добавить пункты меню на русском
│  └─ Language: Russian
│
└─ Создать меню: "Main Menu EN"
   ├─ Добавить пункты меню на английском
   └─ Language: English
```

### Этап 4: Переводы в теме (1-2 часа)

```php
// В файле темы: functions.php

// 1. Регистрация строк для перевода
add_action('init', 'maslovka_register_polylang_strings');
function maslovka_register_polylang_strings() {
    if (function_exists('pll_register_string')) {
        // Общие строки
        pll_register_string('read_more', 'Читать далее', 'Theme');
        pll_register_string('view_all', 'Смотреть все', 'Theme');
        pll_register_string('back_to_list', 'Назад к списку', 'Theme');
        
        // Выставки
        pll_register_string('exhibitions', 'Выставки', 'Theme');
        pll_register_string('current_exhibitions', 'Текущие выставки', 'Theme');
        pll_register_string('past_exhibitions', 'Прошедшие выставки', 'Theme');
        
        // Коллекции
        pll_register_string('collections', 'Коллекции', 'Theme');
        pll_register_string('permanent_collection', 'Постоянная экспозиция', 'Theme');
        
        // События
        pll_register_string('events', 'События', 'Theme');
        pll_register_string('upcoming_events', 'Предстоящие события', 'Theme');
        
        // Формы
        pll_register_string('search', 'Поиск', 'Theme');
        pll_register_string('subscribe', 'Подписаться', 'Theme');
        pll_register_string('contact_us', 'Связаться с нами', 'Theme');
    }
}

// 2. Языковой переключатель
function maslovka_language_switcher() {
    if (function_exists('pll_the_languages')) {
        $args = array(
            'dropdown' => 0, // 0 = список, 1 = dropdown
            'show_names' => 1,
            'display_names_as' => 'slug', // 'name' или 'slug'
            'show_flags' => 1,
            'hide_if_empty' => 0,
            'hide_current' => 0,
        );
        pll_the_languages($args);
    }
}

// 3. Хелпер для получения переведенной строки
function __t($string) {
    if (function_exists('pll__')) {
        return pll__($string);
    }
    return $string;
}
```

```php
// В шаблонах темы (например, header.php):

<!-- Языковой переключатель -->
<div class="language-switcher">
    <?php maslovka_language_switcher(); ?>
</div>

<!-- Использование переводов -->
<h2><?php echo __t('Выставки'); ?></h2>
<a href="<?php echo get_post_type_archive_link('collections'); ?>">
    <?php echo __t('Смотреть все коллекции'); ?>
</a>
```

### Этап 5: Создание переводов контента (по мере наполнения)

```bash
1. Создание нового поста:
   ├─ Posts → Collections → Add New
   ├─ Заполнить контент на русском
   ├─ Опубликовать
   └─ В боковой панели: Language → [+ Add translation] EN
      └─ Откроется редактор английской версии
         ├─ Заполнить контент на английском
         └─ Опубликовать

2. Связь переводов:
   - Polylang автоматически связывает посты
   - В списке постов видно: 🇷🇺 🇬🇧 флажки

3. ACF поля:
   - В редакторе появятся табы: [Russian] [English]
   - Переключайтесь между табами для заполнения полей
```

---

## 📊 Что получаем в результате

### Для пользователей:
```
✅ Чистые понятные URL: maslovka.org/en/exhibitions/
✅ Автоматическое определение языка браузера
✅ Переключатель языка на каждой странице
✅ Возможность поделиться ссылкой на конкретном языке
✅ Закладки работают правильно
```

### Для SEO:
```
✅ Правильные hreflang теги (автоматически):
   <link rel="alternate" hreflang="ru" href="https://maslovka.org/kollekcii/" />
   <link rel="alternate" hreflang="en" href="https://maslovka.org/en/collections/" />

✅ Правильная индексация в Google
✅ Отдельные мета-описания для каждого языка
✅ Отдельные sitemap для каждого языка
✅ Нет дублирования контента
```

### Для разработки:
```
✅ Все переводы в одной базе данных
✅ Простое API для работы с переводами
✅ Git-friendly (настройки экспортируются)
✅ Легко добавить третий язык в будущем
```

### Для контент-менеджера:
```
✅ Понятный интерфейс в админке
✅ Видно какие посты переведены (флажки)
✅ Можно копировать контент между языками
✅ Bulk-операции работают
```

---

## 💰 Стоимость и лицензирование

```
Polylang PRO:
├─ Single Site: €99/год
├─ 5 Sites: €199/год
└─ Unlimited Sites: €299/год

Рекомендация: Single Site (€99/год)

Включает:
✅ ACF интеграция
✅ Share slugs между языками
✅ Duplicate content при создании перевода
✅ WooCommerce support (если понадобится)
✅ Техподдержка
✅ Обновления
```

---

## ⚠️ Важные замечания

### О чем нужно помнить:

1. **Переводы не автоматические**
   - Каждый пост нужно переводить вручную
   - ACF поля тоже переводятся вручную
   - Но это дает полный контроль над качеством

2. **Изображения**
   - Featured Image можно использовать одно на оба языка
   - Или загружать разные (если есть текст на изображении)

3. **Даты и числа**
   - Даты могут автоматически форматироваться по локали
   - В ACF Date Picker укажите формат: `d/m/Y` или `m/d/Y`

4. **Slug (ЧПУ)**
   - С Polylang PRO можно использовать одинаковые slugs
   - Или разные (рекомендуется): `postoyannaya` vs `permanent`

5. **Меню**
   - Для каждого языка создается отдельное меню
   - Нужно вручную связать пункты меню с переводами страниц

---

## 🎯 Итоговая рекомендация

### ✅ ДЕЛАЙТЕ СЕЙЧАС:
```
1. Структура уже готова (3 CPT + 1 taxonomy + ACF)
2. Контента еще мало → легко настроить
3. Избежите головной боли в будущем
4. Оценка времени: 3-4 часа на настройку
```

### ✅ ИСПОЛЬЗУЙТЕ поддиректории `/en/`:
```
1. Лучший вариант для SEO
2. Стандарт индустрии (90% сайтов)
3. Проще для пользователей
4. Работает с кешированием
5. Google рекомендует именно этот подход
```

### ❌ НЕ ИСПОЛЬЗУЙТЕ `?lang=en`:
```
1. Проблемы с SEO (плохая индексация)
2. Проблемы с кешированием (CDN, браузеры)
3. Не рекомендуется Google
4. Плохой UX (пользователи не понимают URL)
5. Проблемы с соцсетями и Open Graph
```

---

## 📚 Полезные ссылки

- [Polylang PRO](https://polylang.pro/) - официальный сайт
- [Документация Polylang](https://polylang.pro/doc/) - полная документация
- [ACF + Polylang](https://polylang.pro/doc/working-with-acf-pro/) - интеграция
- [Google о многоязычности](https://developers.google.com/search/docs/specialty/international/localized-versions) - рекомендации Google
- [hreflang теги](https://yoast.com/hreflang-ultimate-guide/) - подробный гайд

---

## 🔄 Следующие шаги

1. **Сейчас:**
   - [ ] Купить лицензию Polylang PRO (€99/год)
   - [ ] Установить и настроить плагин
   - [ ] Настроить языки и URL-структуру
   - [ ] Включить переводы для CPT и таксономий

2. **На этой неделе:**
   - [ ] Добавить языковой переключатель в тему
   - [ ] Обернуть строки в `pll__()` в шаблонах
   - [ ] Создать меню для обоих языков
   - [ ] Протестировать на локальном окружении

3. **До production:**
   - [ ] Перевести основные страницы (About, Contacts, etc.)
   - [ ] Перевести первые 3-5 записей каждого типа (для теста)
   - [ ] Настроить hreflang в HTML
   - [ ] Проверить работу на DEV-сервере

4. **После production:**
   - [ ] Постепенно переводить контент по мере наполнения
   - [ ] Добавить переводы в Google Search Console
   - [ ] Мониторить индексацию обоих языков

---

**Готовы начать внедрение?** Это займет ~3-4 часа работы, но сэкономит недели в будущем! 🚀
