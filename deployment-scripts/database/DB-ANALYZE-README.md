# 📊 Database Analysis Script

Скрипт для анализа базы данных WordPress с подробными метриками и рекомендациями по оптимизации.

## 🚀 Использование

```bash
# Анализ локальной базы данных (в Docker)
./deployment-scripts/database/db-analyze.sh local

# Анализ DEV базы данных
./deployment-scripts/database/db-analyze.sh dev

# Анализ PROD базы данных
./deployment-scripts/database/db-analyze.sh prod

# Без интерактивного просмотра
./deployment-scripts/database/db-analyze.sh local --no-view
```

## 📋 Что анализируется

### 1. **Общая информация**
- Размер базы данных
- Количество таблиц
- Размеры каждой таблицы (топ 10)
- **Фрагментация таблиц** ⭐
  - Общий объем фрагментации
  - Список таблиц с фрагментацией >1 MB
  - Процент фрагментации для каждой таблицы

### 2. **Autoload опции**
- Общий размер autoload опций
- Количество autoload опций
- Список больших опций (>50 KB)
- ⚠️ Предупреждение при размере >1 MB

### 3. **Transient кеш**
- Количество transient записей
- Размер transient данных
- ⚠️ Предупреждение при >100 записях

### 4. **Посты и ревизии**
- Опубликованные посты и страницы
- Количество ревизий
- Черновики и удаленные
- ⚠️ Предупреждение при >100 ревизиях

### 5. **Custom Post Types**
- Статистика по каждому типу постов
- Количество записей каждого типа

### 6. **Комментарии**
- Одобренные комментарии
- Спам и удаленные
- Ожидающие модерации
- ⚠️ Предупреждение о спаме

### 7. **Orphaned данные**
- Потерянные postmeta (без постов)
- Потерянные termmeta (без терминов)
- Потерянные commentmeta (без комментариев)

### 8. **Таксономии**
- Статистика по всем таксономиям
- Количество терминов

### 9. **Рекомендации по оптимизации**
- Автоматические рекомендации на основе анализа
- Приоритеты действий

## 📂 Отчеты

Отчеты сохраняются в двух форматах:

### Текстовый отчет
```
wordpress/database/reports/db-analysis-{environment}-{timestamp}.txt
```
Человеко-читаемый формат с подробной информацией.

### JSON отчет
```
wordpress/database/reports/db-analysis-{environment}-{timestamp}.json
```
Машино-читаемый формат для автоматизации.

Пример JSON:
```json
{
  "environment": "local",
  "database": "wordpress_db",
  "timestamp": "2025-11-09T16:02:36Z",
  "metrics": {
    "database_size_mb": 26.02,
    "table_count": 18,
    "fragmentation_mb": 11.0,
    "fragmented_tables": 3,
    "autoload": {
      "size_kb": 0.00,
      "count": 1
    },
    "transients": {
      "count": 68,
      "size_kb": 808.67
    },
    "posts": {
      "published": 0,
      "pages": 13,
      "revisions": 556,
      "drafts": 62,
      "trash": 2
    },
    "comments": {
      "approved": 0,
      "spam": 8,
      "trash": 0,
      "pending": 0
    },
    "orphaned": {
      "postmeta": 0,
      "termmeta": 0,
      "commentmeta": 0
    }
  },
  "recommendations_count": 3
}
```

## 🔧 Типичные проблемы и решения

### Фрагментация таблиц (>10 MB)
**Причина:** Удаление записей оставляет "дыры" в таблицах.

**Проверка:**
```sql
SELECT 
    table_name, 
    ROUND(data_free / 1024 / 1024, 2) as fragmented_mb,
    ROUND((data_free / (data_length + index_length + data_free)) * 100, 1) as fragmented_percent
FROM information_schema.TABLES 
WHERE table_schema = 'wordpress_db' 
  AND engine = 'InnoDB'
  AND data_free > 1048576
ORDER BY data_free DESC;
```

**Решение:**
```sql
-- Оптимизировать конкретную таблицу
OPTIMIZE TABLE wp_posts;

-- Оптимизировать все фрагментированные таблицы
OPTIMIZE TABLE wp_posts, wp_postmeta, wp_options;
```

**Эффект:** 
- Уменьшение размера БД на 10-50%
- Ускорение запросов на 20-30%
- Экономия дискового пространства

### Большой autoload (>1 MB)
```sql
-- Найти большие опции
SELECT option_name, LENGTH(option_value) / 1024 as size_kb 
FROM wp_options 
WHERE autoload = 'yes' 
ORDER BY size_kb DESC 
LIMIT 10;

-- Отключить autoload для конкретной опции
UPDATE wp_options 
SET autoload = 'no' 
WHERE option_name = 'название_опции';
```

### Много transient
```sql
-- Удалить все transient
DELETE FROM wp_options 
WHERE option_name LIKE '_transient_%' 
   OR option_name LIKE '_site_transient_%';
```

### Много ревизий
```sql
-- Удалить старые ревизии (>30 дней)
DELETE FROM wp_posts 
WHERE post_type = 'revision' 
  AND post_modified < DATE_SUB(NOW(), INTERVAL 30 DAY);
```

### Спам комментарии
```sql
-- Удалить спам
DELETE FROM wp_comments 
WHERE comment_approved = 'spam';
```

### Orphaned данные
```sql
-- Удалить orphaned postmeta
DELETE pm FROM wp_postmeta pm
LEFT JOIN wp_posts wp ON wp.ID = pm.post_id
WHERE wp.ID IS NULL;
```

## ⚡ Оптимизация таблиц

После очистки всегда запускайте оптимизацию:

```sql
OPTIMIZE TABLE wp_options, wp_postmeta, wp_posts, 
               wp_comments, wp_termmeta, wp_terms;
```

## 📊 Пример использования

```bash
# 1. Запустить анализ
./deployment-scripts/database/db-analyze.sh local

# 2. Просмотреть отчет
cat wordpress/database/reports/db-analysis-local-*.txt

# 3. Применить рекомендации из отчета
# (через phpMyAdmin или WP-CLI)

# 4. Повторить анализ для проверки
./deployment-scripts/database/db-analyze.sh local
```

## 🔒 Безопасность

- **Всегда делайте бэкап перед оптимизацией!**
- Отчеты добавлены в `.gitignore` (могут содержать чувствительную информацию)
- Для удаленных серверов требуется SSH доступ

## 🎯 Регулярность

Рекомендуется запускать анализ:
- **Локально**: перед каждым деплоем
- **DEV**: раз в неделю
- **PROD**: раз в месяц или после крупных изменений
